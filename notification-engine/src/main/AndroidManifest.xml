<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application>
        <receiver
            android:name="com.swift.browser.notificationengine.NotificationReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.swift.browser.notificationengine.download.DownloadNotificationActionReceiver"
            android:exported="false" />

        <receiver
            android:name="com.swift.browser.notificationengine.media.MediaNotificationReceiver"
            android:exported="true" />

        <service
            android:name="com.swift.browser.notificationengine.download.DownloadForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
